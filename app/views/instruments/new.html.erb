<h1>Add a new Instrument</h1>
<%= form_for @instrument, :html => {:multipart => true} do |f| %>
  <fieldset>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </fieldset>
  <%= f.file_field :photo %>
  <%= f.submit %>
<% end %>